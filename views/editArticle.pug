doctype html
html(lang="en")
    head
        meta(charset='UTF-8')
        meta(name='viewport' content='width=device-width, initial-scale=1, shrink-to-fit=no')
        title Административен панел - Редактиране на статия
        link(rel='stylesheet' href='https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css')
        link(rel='stylesheet' href='https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.2/font/bootstrap-icons.min.css')
        script(src='https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js')
        script(src='https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.min.js')
        style.
            body {
                font-family: 'Open Sans', sans-serif;
            }
            .navbar {
                background-color: #EDE8E3;
            }
            .footer {
                background-color: #E3E9ED;
            }

    body
        header
            nav.navbar.navbar-expand-lg.navbar-light.bg-light
                .container-fluid
                    a.navbar-brand(href='/') OurPets
                    button.navbar-toggler(type='button' data-bs-toggle='collapse' data-bs-target='#navbarNav' aria-controls='navbarNav' aria-expanded='false' aria-label='Toggle navigation')
                        span.navbar-toggler-icon
                    #navbarNav.collapse.navbar-collapse
                        ul.navbar-nav.ms-auto
                            li.nav-item
                                a.nav-link(href='/admin/articles') Обратно към статии

    main.d-flex.justify-content-center.align-items-center.text-start.mx-3(style="height: 50rem;")
      .card.mt-3.my-3.shadow
        .card-body.mt-3
          .row.pt-3.mx-0
                h1.text-center Редактиране на статия
                form(action=`/admin/article/update/${article.id}`, method="post")
                    .mb-3
                        label(for='title' class='form-label') Заглавие
                        input#title.form-control(type='text', name='title', value=article.title, required=true)
                    .mb-3
                        label(for='content' class='form-label') Съдържание:
                        textarea#content.form-control(name='content', required=true)=article.content
                    .mb-3
                        .form-group
                         label(for="picture") Изображение (URL):
                         input#picture.form-control(type="text" name="picture" value=article.picture)
                    .d-flex.justify-content-between.align-items-center
                        button.btn.btn-primary(type='submit') Редактирай
                        a.btn.btn-danger(href="/adverts") Откажи

    footer
        .container-fluid.text-center.py-3
            span.text-muted © 2024 OurPets. Сайтът е създаден с учебна цел!
