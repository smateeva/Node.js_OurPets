doctype html
html(lang="bg")
    head
        meta(charset="UTF-8")
        meta(name="viewport" content="width=device-width, initial-scale=1")
        title Административен панел - Добавяне на статия
        // Using Bootstrap 5 for consistency with other parts
        link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css")
        script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js")
        style.
            body {
                font-family: 'Open Sans', sans-serif;
            }
            .navbar {
                background-color: #EDE8E3;
            }
            .footer {
                background-color: #E3E9ED;
            }
            .container {
                margin-top: 2rem;
            }
    body
        header
            nav.navbar.navbar-expand-lg.navbar-light
                .container
                    a.navbar-brand(href='/admin') Административен панел
                    button.navbar-toggler(type='button' data-bs-toggle='collapse' data-bs-target='#navbarNavAltMarkup' aria-controls='navbarNavAltMarkup' aria-expanded='false' aria-label='Toggle navigation')
                        span.navbar-toggler-icon
                    .collapse.navbar-collapse#navbarNavAltMarkup
                        .navbar-nav.ms-auto
                            li.nav-item
                                a.nav-link.active(href='/admin/articles') Управление на статии
                            li.nav-item
                                a.nav-link(href='/admin/logout') Изход

        main
            .container
                h1 Добави статия
                form(method="POST" action="/admin/article/new")
                    .mb-3
                        label(for="title") Заглавие:
                        input#title.form-control(type="text" name="title" required=true)
                    .mb-3
                        label(for="content") Съдържание:
                        textarea#content.form-control(name="content" required=true rows="5")
                    .mb-3
                        .form-group
                        label(for="picture") Изображение (URL):
                        input#picture.form-control(type="text" name="picture" placeholder="http://example.com/image.jpg")
                    button.btn.btn-primary(type="submit") Публикувай

        footer
            .container.text-center.py-4
                span.text-muted © 2024 OurPets. Сайтът е създаден с учебна цел!

        // Bootstrap JS
        script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js")
